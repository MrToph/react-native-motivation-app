const devSettings = {
  logRedux: true,
}
const apiSource = 'http://cmichel.io/test/api.html'
const dayKeys = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat']

export {
    apiSource,
    devSettings,
    dayKeys,
}
